import { Layout } from '../components';
import Settings from '../features/settings/settings-feature';

const SettingsPage = () => {
  return (
    <Layout>
      <Settings />
    </Layout>
  );
};

export default SettingsPage;
